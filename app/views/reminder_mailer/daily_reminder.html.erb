<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; background: #FFFDF0; color: #3D2B00; margin: 0; padding: 0; }
      .wrapper { max-width: 560px; margin: 0 auto; background: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 8px rgba(61,43,0,0.1); }
      .header { background: linear-gradient(135deg, #F5C518 0%, #F0B300 100%); padding: 1.5rem 2rem; }
      .header h1 { margin: 0; font-size: 1.4rem; color: #3D2B00; }
      .header p  { margin: 0.25rem 0 0; font-size: 0.9rem; color: #5C4200; }
      .body { padding: 1.5rem 2rem; }
      .greeting { font-size: 1rem; margin-bottom: 1rem; }
      .task-list { list-style: none; padding: 0; margin: 0 0 1.5rem; }
      .task-item { display: flex; align-items: flex-start; gap: 0.6rem; padding: 0.6rem 0; border-bottom: 1px solid #F5DFA0; font-size: 0.95rem; }
      .task-item:last-child { border-bottom: none; }
      .task-bullet { color: #2A9D8F; font-size: 1.1rem; line-height: 1.4; flex-shrink: 0; }
      .task-time { font-size: 0.8rem; color: #92723A; background: #FEF3C7; border-radius: 4px; padding: 0.1rem 0.4rem; white-space: nowrap; }
      .cta { text-align: center; margin: 1.5rem 0 0.5rem; }
      .btn { display: inline-block; background: #F5C518; color: #3D2B00; text-decoration: none; padding: 0.6rem 1.4rem; border-radius: 6px; font-weight: 600; font-size: 0.95rem; }
      .footer { padding: 1rem 2rem; background: #FEF3C7; font-size: 0.8rem; color: #92723A; text-align: center; }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="header">
        <h1>☀️ Brighter Day</h1>
        <p>Daily Task Reminder</p>
      </div>
      <div class="body">
        <p class="greeting">Hi <%= @user.email.split("@").first %>,</p>
        <p>Here are your tasks scheduled for <strong><%= @tomorrow.strftime("%A, %B %d, %Y") %></strong>:</p>
        <ul class="task-list">
          <% @tasks.each do |task| %>
            <li class="task-item">
              <span class="task-bullet">●</span>
              <span>
                <%= task.title %>
                <% if task.time_of_day %>
                  <span class="task-time"><%= task.time_of_day.strftime("%I:%M %p") %></span>
                <% end %>
                <% if task.notes.present? %>
                  <br><small style="color:#92723A;"><%= task.notes %></small>
                <% end %>
              </span>
            </li>
          <% end %>
        </ul>
        <div class="cta">
          <a class="btn" href="<%= planner_items_url %>">Open Your Planner</a>
        </div>
      </div>
      <div class="footer">You're receiving this because you have tasks scheduled for tomorrow. Log in to Brighter Day to manage your tasks.</div>
    </div>
  </body>
</html>
